dependencies {
    compile project(':main:ip')
    compile project(':main:feature')
    compile project(':main:geo')

    testCompile project(':main:io')
}

idea {
    module {
        name = "BoofCV Calibration"
    }
}